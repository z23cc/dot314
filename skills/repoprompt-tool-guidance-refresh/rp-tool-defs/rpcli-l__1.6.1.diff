382,386d381
< - list: List snapshots
< - get: Read a snapshot by snapshot_id (or "current")
< - delete: Delete a snapshot
< - prune: Delete snapshots older than N days
< 
400a396,401
> Repo targeting:
> - Defaults to the first loaded root folder's git repo
> - Use repo_root (path or loaded root name) to target a specific repo
> - Use repo_roots (array of paths or names) for explicit multi-root operations
> - Results include per-repo data and aggregate totals when multiple repos targeted
> 
405c406
<   op (string, required): status | diff | log | show | blame | list | get | delete | prune
---
>   op (string, required): status | diff | log | show | blame
420,422d420
<   days (integer)
<     For list/prune: filter by days
< 
440,442d437
<   limit (integer)
<     For list: max results
< 
466a462,464
>   repo_key (string)
>     Repository key (optional alternative to repo_root)
> 
468c466,467
<     Repository root (defaults to active workspace root)
---
>     Repository root path or loaded root name (defaults to first loaded
>     root)
470,471c469,471
<   reuse (boolean)
<     Reuse existing snapshot if fingerprint matches
---
>   repo_roots (array<string>)
>     Multiple repository root paths or names (for multi-root
>     operations)
476,477d475
<   snapshot_id (string)
<     Snapshot ID for get/delete or override for diff
479d476
< 
